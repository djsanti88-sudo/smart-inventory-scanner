# SDD progress - Tire PN affix canonicalization + Point S pilot backfill

Plan: docs/superpowers/plans/2026-07-15-tire-pn-canonicalization-and-pilot-backfill.md
Branch: feat/shopware-reconcile-pn-fill
Base for reviews (recorded before Task A1 dispatch): 24c742b
Priority: on-hand stock list ~/Downloads/tires_inventory_379.csv (379 rows) is the focus for Workstream B.

Trust spine (owner corrections, binding): generic affix/core is DISCOVERY-only, never auto-counts;
brand+size/affix-core/model-token never ATTACH a barcode (human confirm or captured exact barcode only).

- Task A1: complete (commit 69cd271, review clean; 9/9 pass). MINOR (plan-mandated, defer): basePartNumberKey .trim() redundant vs trailing s strip - intentionally mirrors normPartKey.
- Task A2: complete (commit 35c4b34, review clean; reconcile suite 47/47, tsc clean; pre-existing test-mock fix verified legitimate).
- Task A3: complete (commit 246c8d7, review clean; 11/11 route tests, red-first confirmed).
- Task A4: complete (commit 7b71347, review clean; 29/29 pass, guards green first run, ZERO production change - trust boundary locked). WORKSTREAM A DONE.
- Task B1: complete (commit bf177b1; controller-reviewed: read full script + LIVE run vs 379 on-hand list). RESULT: 47 matched candidates (37 via affix-core - the canonicalization earned its keep, 10 exact PN+brand+size), 13 ambiguous, 284 unmatched (house brands absent from corpus -> sourcing funnel), 35 non_tire. All 47 review-gated, affix-core tier tagged. Worklist: docs/pilot/point-s-backfill-worklist.csv.

Task A2 base recorded: 69cd271

Task A3 base recorded: 35c4b34

Task A4 base recorded: 246c8d7

Task B1 base recorded: 7b71347

## FINAL REVIEW: READY TO MERGE (opus whole-branch, range 05a1694..bf177b1).
Both owner trust constraints CONFIRMED (affix-core discovery-only never auto-counts; nothing attaches on brand+size/affix-core). No Critical/Important. Gates: full vitest 2272 pass/0 fail/32 skip, tsc --noEmit exit 0, harness live-run verified.
Minor 1 (deferred, dev harness only): tierOf() labels Step-2 identity matches as candidate_pn_brand_size (should read candidate_identity) - no trust impact, all rows still (confirm).
Minor 2: informational only (core fan-out can safely demote an exact affixed match to ambiguous - Needs Review over wrong guess).
INTEGRATION (merge/PR/push) is OWNER-GATED - not done. Branch feat/shopware-reconcile-pn-fill, all work committed.


# PLAN: barcode-trust-gate-phase1 (2026-07-15, branch feat/barcode-trust-gate, base 47351c5)
Task 1: complete (commits 51462ca+dde737a fix, review clean after 1 Important fix: pnDerived padding false-positive)
Task 2: complete (commit 0454db2, review clean; Minor noted: gatePhysicalLabel non-GTIN branch omits partNumber - stylistic only)
Task 3: complete (commit 959fbca, Opus review clean, all 6 probes pass; fixture fixes verified genuine; no tracked deletions)
Angle A (adversarial probe): 2 findings - CRITICAL zero-padded placeholder bypass (9 variants reach verified), IMPORTANT pnDerived O(n3) DoS; invariants 1/3/5 HELD over 50k fuzz. Fix dispatched.
Angle A fixes: complete (commit 0ead1f2, 35/35 + 94/94 upc dir; placeholder bypass closed incl. physical_scan; 10k-input grade now <500ms vs ~27s)
Task 4: complete (commit 6db425f, Opus review clean; site census 3 decode sites scrubbed / matched-product+empty sites untouched; implementer correctly fixed brief phantom-code error)
Task 5: complete (commit fc5a15f, review pending; mirror hardened to post-probe logic)
Task 5: review clean (drift test traced against 3 drift scenarios; row.gtin deviation verified correct across all 4 call sites; reviewer re-ran dt-harvest 148/148)
Angle C (test audit): 3 Important + 4 Minor found; fixer commit 90f2ce1 - UPC-E expansion recall bug FIXED (real correctness bug), law pinned at N=10, case-pack + edge pins; 141/141
Angle B (browser law proof): 3/3 PASS commit d3168bf - random code, bad-check-digit, scan10=count10 all appear+count; screenshots in e2e/proof/; no law violation
Angle D (qa bots): tire 1/1 + data 1/1 PASS at d3168bf (isolated worktree, mock backend confirmed, no live calls); Falken-never-Camel 8/8 shapes
FINAL REVIEW: READY TO MERGE (Opus whole-branch 47351c5..cb6a721, 12 commits). All 7 laws verified w/ live probes; cross-task UPC-E/mirror/scrub integration resolved. 2 non-blocking Minors: (1) .mjs/TS isPlaceholderBarcode parity fuzz (drift latent+unreachable today), (2) import-layer UPC-E test (live-probed OK). Gates: vitest 2353/0, tsc 0, lint pre-existing only. MERGE/PUSH OWNER-GATED.

# PLAN: phase1-ledger (2026-07-19, branch feat/phase1-ledger, plan docs/superpowers/plans/2026-07-19-phase1-ledger.md)
Base after plan-docs commit: 219deb7
Task 1: complete (219deb7..2aa6207, review clean, Opus approved; 3/3 new + full 2485/0, tsc clean). Minors deferred to final review: synthetic replay count id not order-stable (cosmetic, nothing asserts on it); optional interleaved-duplicate coverage.
Task 2 base recorded: 2aa6207
Task 2: complete (2aa6207..75ce444, review clean, Opus approved; controller-authorized test correction: two-half failure-then-real-ack test, plan doc corrected in-commit; RED 6/6 -> GREEN 6/6, full 2491/0, tsc clean). Minors deferred: stricter toBe('error') assertion optional; applyDecodeFallback entry-point coverage (Task 4/7 will drive it).
Task 3 base recorded: 75ce444
Task 3: complete (75ce444..d83b346, review clean, Opus approved; RED static-lock -> GREEN, full 2493/0, tsc clean). Minors deferred: lock marker is string-literal-brittle (inherited keySafety idiom, sanctioned); :2614/:2887 sites source-locked only (by-design, live-decode paths reach them in Task 4/7 tests).
Task 4 base recorded: d83b346
Task 4: complete (d83b346..f57814c, review clean, Opus approved; helper pure + 3 sites delegate, zero copy-paste left; site-2 fixture deviation adjudicated legitimate route-to-branch fix; 3 RED -> 14 GREEN, full 2496/0, tsc clean). Minor deferred: identity-merge-rule refactor would time-out not assert-fail in the two integration tests (fragility note for final review).
Task 5 base recorded: f57814c
Task 5: complete (f57814c..c479834, review clean, Opus approved; ordering constraint verified against live guard; security-test change adjudicated JUSTIFIED all 3 prongs - guarantee retained+strengthened 2->5 assertions; 3/3 focused RED->GREEN, full 2499/0, tsc clean). Minor deferred: residual feed-trim branch proven by reasoning only - candidate targeted test at final review; mixed status/resolverStatus pairing wants a one-line comment.
Task 6 base recorded: c479834
Task 6: complete (c479834..754d6c5, review clean, Opus approved; 4 named risks clean by design - timer finally-clear, structural late-resolve immunity, min() budget clamp, existing test comment-only; 23/23 ladder fake-timers 168ms, full 2503/0, tsc clean). Minor deferred to final review: attach no-op .catch to abandoned race loser (latent unhandled-rejection if a rung rejects post-abandon; real runners resolve).
Task 7 base recorded: 754d6c5
Task 7: complete (754d6c5..8bd07f7, 2 commits, review clean after 1 fix round; Opus approved. Importants FIXED: retry-idempotency now server-side exactly-once w/ force-pending sentinels + documented loud-failure proof; pending-window books-balance added. Sentinels added CONFLICT+POST-MARKWRONG. 14/14 in 2.15s, full 2517/0, tsc clean; reviewer withdrew own miscount on evidence). Minor deferred to final review: POST-MARKWRONG lacks positive finalCounts.length>0 companion (Task 5 suite carries the positive proof).
Task 8 base recorded: 8bd07f7
Task 8: complete (8bd07f7..7ff2992, review clean, Opus approved; 4 identity-outcome classes, CONFLICT proven non-vacuous by live mutation; 4/4 + golden 2/2, full 2521/0, tsc clean). Minors deferred to final review: EXAMPLE could add review-queue presence assertion; CONFLICT could add positive safe-provisional companion (arguably Task 7 domain).
Task 9 base recorded: 7ff2992
Task 9: spec committed (7ff2992..849b179, 2/2 both viewports, non-vacuous proven; e2e suite 41 pass + 1 pre-existing goupc-ladder flake reproduced in isolation). DISCOVERY: real D2-family bug - markWrong on a PROVISIONAL inflates total (unordered provRow .find at ~:4602 grabs old provisional; second provisional shares primaryBarcode). Controller-verified mechanism in source. FIX DISPATCHED to Task 5 implementer (TDD: provisional-wrong unit test + deterministic selection + e2e scenario). Task 9 review HELD until fix lands - one gate covers spec+fix.
Task 9 + provisional-fix: complete (7ff2992..4990687, 2 commits, Opus approved; all 5 named risks verified clean - signature change safe on both paths + 3 discarding callers, ambiguous .find REPLACED id-keyed, RED 'expected 3 to be 2' genuine, DOM+store dual assertions both scenarios both viewports 4/4, gap note truthful). markWrongTransfer 4/4, ledgerInvariants 14/14, full 2522/0, tsc clean. task-9-report.md superseded-note added by controller. Pre-existing goupc-ladder.spec.ts cap-timing flake disclosed + reproduced in isolation (NOT ours - candidate for final-review note).
Task 10 base recorded: 4990687
Task 10: complete (4990687..b44d85a; test:ledger gate 8 files/40 tests 3.14s PASS; full unit 2522/0 23.6s; tsc clean; golden 2/2; e2e 43/1 - the 1 = goupc-ladder cap-timing, DETERMINISTIC pre-existing (file untouched since 9226f3d, pre-branch), isolated re-run identical; lint 85 problems all pre-existing via git stash proof). Branch feat/phase1-ledger 13 commits, never pushed. ALL 10 TASKS COMPLETE -> final whole-branch review next.
Sweep: complete (e2e945a - honest ladder error reasons w/ timeout sentinel + focused test, residual pairing comment; 24/24 + test:ledger 41/41 + tsc clean). Sweep agent's 'stray backslash' flag CONTROLLER-DISPROVEN by direct read (lines 4684/4686 normal). Master-plan P3 handoff note committed.
FINAL WHOLE-BRANCH REVIEW: READY TO MERGE (Opus, 62d220e..b44d85a+sweep; 0 Critical, 0 Important-on-branch; all 8 Minors triaged DEFER w/ evidence; gates live-verified at HEAD; persist quantityDelta landmine documented for P2/P3). PHASE 1 COMPLETE - 15 commits on feat/phase1-ledger, NEVER pushed. Merge + Phase 2 = owner decisions.

MERGED: feat/phase1-ledger -> feat/decode-ladder-goupc (e6f2817, --no-ff, owner-approved local merge, NOT pushed); post-merge gates green (test:ledger 41/41 2.38s, tsc clean).
# PHASE 2 KICKOFF (2026-07-19, SPEED MODE - owner order: max parallelism, token spend OK)
3 parallel scouts dispatched: auth surface / tenancy+persist / rules+two-DB -> p2-scout-{auth,tenancy,rules}.md -> Opus plan-writer next.
OWNER ORDER logged: autonomous progression (no per-phase approval waits), end-of-phase ULTRA REVIEW (multi-agent Workflow) mandatory, merge locally + continue; caps 5 subagents / 4 shells; push+deploy+paid still gated.
P2 plan 3-angle panel COMPLETE: feasibility EXECUTABLE-WITH-EDITS (3 import blockers, L12 charge bug, mechanics ruling: single checkout + orchestrator-serialized commits), spec APPROVE-WITH-EDITS (Task 15 regression, type fix, both deferrals judged legitimate), security EDITS-REQUIRED (2 HIGH: split-brain chokepoint - only 1 of 3 legacy env reads migrated; scanContext/autoCountNonPublic unclamped; + migration ownership, sign-out key residue, business-switch isolation). 3 findings independently x2-corroborated. 18-finding consolidated revision dispatched to plan drafter.
OWNER CHECKPOINT ORDER: when the P2 plan revision + verification is DONE, STOP and tell the owner to /compact (Fable cannot compact itself). Resume-from-disk after compact: this ledger + docs/superpowers/plans/2026-07-19-phase2-accounts.md + p2-scout files + memory. Post-compact next step: P2 execution, single checkout, parallel tracks T1-T4, orchestrator-serialized commits, 5-agent cap.
P2 plan REVISED (17 tasks / 94 steps): all 18 findings structurally fixed per drafter (chokepoint complete 3/3 reads, scanContext+autoCount server-forced, owner-initiated adopt + legacy delete, resetForSignOut removes uid key + re-points anon, paidComputeCharged threads the single :1229 charge site, single-checkout mechanics, Task 15 body preserved). Loop-2 fresh-eyes verifier dispatched (final gate). WHEN VERIFIER RETURNS CLEAN: plan is DONE -> STOP and tell owner to /compact per checkpoint order.
P2 PLAN DONE + COMMITTED: loop-2 verifier confirmed all 18 fixes real (45/45 anchors), found R1 duplicate cacheMsg (compile-stopper) + R2 stale FinalCountTable lines + R3 closure note - all three patched by controller directly and verified in-file. Plan = docs/superpowers/plans/2026-07-19-phase2-accounts.md, 17 tasks / 94 steps, EXECUTION-READY. NEXT AFTER COMPACT: task 8 on the board - P2 execution, single checkout, S1 authMode chokepoint first (Tasks 1-2), then tracks T1(3,4,5)/T2(9,10,11,12)/T3(6->7->8)/T4(13;14->15) parallel, orchestrator-serial commits, Sonnet implementers + Opus reviewers, then Tasks 16-17, ultra review, merge.
Task 1-2 base recorded: 564ad52
Tasks 1-2 (S1 chokepoint): implemented, committed 658220b + 254565c. CONTROLLER CATCH: bare `auth` .gitignore pattern (Turso WAL artifacts) silently excluded src/services/auth/ from the first commit - root-anchored to /auth,/auth-wal and files tracked in 254565c. Root artifacts verified still ignored. Implementer gates: authMode 4/4 + auth suite 9/9, tsc clean, zero legacy reads outside chokepoint (grep-proven).
WAVE 1 DISPATCHED (5-agent cap): Opus review of Tasks 1-2 (package review-564ad52..254565c.diff) PIPELINED with 4 track openers - Task 3 (T1, sonnet), Task 6 (T3, sonnet), Task 9 (T2, haiku), Task 13 (T4, sonnet). All disjoint files, focused tests only, no subagent commits; controller runs repo-wide gates at serialization.
Task 9: complete (impl haiku 4/4, controller-verified verbatim vs brief, committed) - review folded into track-2 batch review
Tasks 1-2: complete (564ad52..254565c, Opus review clean - spec PASS, quality APPROVED, 0 Critical/Important; 2 Minors deferred to final review: stale line-number refs in module comment, strict-"1" legacy check intentional). gitignore fix independently verified by reviewer.
Task 10 base recorded: 4c0ae63
Task 13: complete (impl sonnet; RED 2 -> GREEN 2, regression resolver+aliasMatcher 42/42; controller-verified diff verbatim vs brief, pass-through preserves behavior; committed). Review policy this phase: trivial verbatim-transcription tasks (9, 13, 14, 11) get controller diff-verification + track batch review; heavy tasks (6, 10, 15, 16) get dedicated Opus task reviews. All roll into final whole-branch review + ultra review.
Task 11 + Task 14 dispatched (haiku, disjoint files). In flight: 3 (T1 auth lib), 6 (T3 persist), 11, 14.
Task 6: complete-pending-review (impl sonnet; 6/6 new + 13/13 regression; controller-verified diff verbatim vs brief; committed 4628772; Opus review IN FLIGHT).
Task 11: complete (impl haiku 2/2; controller-verified diff verbatim; committed 679f7c2; tsc spot-gate clean at serialization).
Task 14: complete (impl haiku 2/2; controller-verified verbatim; committed 314ebca).
Task 3: complete (impl sonnet DONE_WITH_CONCERNS; 4/4 via unit project; committed 0eae6e2). TWO REAL IMPLEMENTER CATCHES: (a) vitest.config.ts had NO include glob for src/lib/** - test would silently never run in CI; controller added src/lib/**/*.test.ts to unit project + proved pickup (164ms 4/4). (b) brief's vi.mock snippet had a Vitest-4 TDZ hoisting bug; fixed with repo's vi.hoisted pattern, assertions unchanged.
WAVE 2 DISPATCHED (cap 5): Opus review Task 6 (package review-26ff6fc..4628772.diff) + implementers Task 4 (T1 AuthGuard runtime check, sonnet - file drifted from brief, S1 already swapped the read, dispatch says complete the runtime upgrade), Task 10 (T2 route hardening, sonnet), Task 12 (T2 rules test, sonnet, emulator), Task 15 (T4 PIN wiring, sonnet). Disjoint files verified. Queued next: Task 7 (after T6 review), Task 5 (after T4), Tasks 16-17 last.
OWNER ORDER (mid-wave): "start moving way faster without losing quality" - controller response: cap saturated 5/5 continuously, trivial verbatim tasks get controller diff-verification instead of dedicated review agents, heavy tasks keep Opus reviews, tsc spot-gates at serialization, briefs pre-extracted, reviews pipelined with implementation.
Task 6: review CLEAN (Opus: spec PASS all 7 constraints on-disk-verified, quality APPROVED, 0 Critical/Important) -> Task 7 dispatched (sonnet, scanStore setBusinessContext + emptyTenantState). Slots 5/5: Tasks 4, 7, 10, 12, 15.
Task 4: complete (impl sonnet; RED TDZ-collision baseline explained, GREEN 2/2; cleanup() deviation legitimate - dom project lacks globals:true so RTL auto-cleanup never registers, repo-convention; controller-verified diff = brief Step 3 target; committed).
Task 12: complete (impl sonnet; emulator suite 10 files/47 tests green, both new deny cases proven, rules already enforced invariant - no rules change; plain npm test correctly skips (12 skipped); controller-verified verbatim; committed).
CONTROLLER: vitest dom include gap fixed pre-Task-5 (src/app/**/*.test.tsx was in NO project - same class as Task 3 catch); committed.
Task 7: complete (impl sonnet; RED->GREEN 3/3 new, regression 11/11 incl. isolated cloudDrainRace pass; controller-verified diff verbatim; committed). Task 8 base recorded at this commit.
Task 5: complete (controller-verified, committed 0123591). Task 15: complete-pending-review (committed c58f032, Opus review in flight). Task 10: complete-pending-review (impl sonnet, 49/49 + tsc clean + eslint clean, controller-verified charge closure + 4 computed sites + auth ordering; committed). T1 track batch review (3+4+5) in flight.
Task 15: review CLEAN (Opus PASS/PASS, 0 Critical/Important, 3 Minors deferred to final review: report mislabel, re-confirm on reopen, stale Wrong-PIN error until next submit).
T1 track (Tasks 3+4+5): review CLEAN (Opus PASS/PASS all three, 0 Critical/Important; 3 Minors deferred: trim on reset only, signup toggle exits reset mode silently, shared busy flag; Google->/business vs signin->/scan adjudicated intentional-per-brief).
Task 10: review CLEAN (Opus PASS/APPROVED, adversarial source verification: ordering :217-243 before first quota touch :296/:326, charge closure :1236, 4 computed sites grep-verified 548/578/592/1602, 503 regex byte-equal to resolve-scan; 3 Minors deferred: no platformOwner bypass = safer default, soft/racy cap identical to global, cosmetic env-fallback diff).
Task 8: complete-pending-review (impl sonnet; RED->GREEN 3/3, store suite 381/381 no flake; controller-verified all hunks vs brief incl. Nav no-duplication; committed). Task 16 dispatched pipelined with Task 8 Opus review.
Task 16: complete (impl sonnet; 6/6 both viewports 11.5s, per-uid key removal proven in browser, controller-flagged userId ?? "unset" brief bug fixed as instructed; committed 6009616).
Task 17 step 1: replay test written by controller (verbatim transcription), 1/1 green, committed 70ed13a. Full gate sweep HELD until I1 fix lands.
Task 8: review PASS + APPROVE WITH FINDINGS - IMPORTANT I1 (controller-verified real): coalesced writer resurrects the per-uid key one tick after resetForSignOut (wipe set() queues pending write under the uid name BEFORE the re-point; direct localStorage.removeItem bypasses the wrapper's pending-cancel). No data leak (content = wiped anon baseline) but key-removed invariant breaks + adopt banner silently suppressed for returning same-uid user. Task 16's e2e passed because mock mode never pointed the writer at the uid key - assertion was not exercising the bug class. FIX DISPATCHED (controller-ruled ordering fix: re-point persist BEFORE the wipe set; single-slot coalescer then targets anon key; e2e hardened with post-tick re-check).
I1 FIX: committed 5653e37 (controller-verified ordering diff exact; re-point before wipe, comment rewritten, e2e post-tick guard). Fixer also found PRE-EXISTING flake in sign-out e2e test (stray scan row from mock ai-lookup on page load racing the reset, ~1-in-3, stash-proven baseline) - queued for ultra-review fix wave.
P2 GATES (at 5653e37 + replay test 70ed13a): unit 2564 passed / 34 skipped / 0 failed (268 files, 29s) [P1 close was 2522 -> +42 net new]; lint 85 problems (40e/45w) = EXACTLY the P1 stash-proven pre-existing baseline, zero new; tsc clean; production build SUCCESS (all routes incl. /login /business); test:firebase exit 0 (emulator suites green). IN FLIGHT: full e2e suite (port 3100), I1 re-review (original Task 8 reviewer resumed).
E2E REGRESSION ROOT-CAUSED + FIXED (controller, systematic): full e2e showed 46/4 (goupc-ladder pre-existing + scan/reconcile/variance NEW, reproduced isolated). Debug spec ground truth: products:0 in browser store while alias-matched scan still counted -> count table joined against zero products ("No counts yet"). ROOT CAUSE: e2e fixtures seed a PARTIAL v7 blob (settings-only); the v7->v8 bump (Task 6) made scanStoreMigrate's non-destructive branch run on it for the first time, injecting products:[] which clobbered the seeded initial state on zustand merge (aliases survived - not in the migrate return). CLASS FIX committed: migrate only transforms keys present in the blob (products/scanFeed/settings conditional; countSnapshots stays unconditional per varianceSnapshot documented contract - initial state empty, injection harmless). Regression tests added (partial-blob + feed-normalization-still-works). Store suite 384/384; scan+reconcile+variance+p2-accounts 9/9; tsc clean.
I1 RE-REVIEW: CLOSED (no post-re-point write path to the uid key; wipe write to sis-scan-v1 = intended anon baseline). NEW: N1 (Important, test gap - e2e I1 guard never re-points persist to the uid key, cannot catch the mechanism; strengthen with rehydrateForUid("test-uid") before reset), N2 (Minor - wipe deposits tenant residue into sis-scan-v1 + spurious adopt banner; emptyTenantState scope). N1+N2+stray-scan-row flake => pre-seeded into ultra-review fix wave.
ULTRA REVIEW COMPLETE (Workflow, 14 agents, 5 finders + opus adversarial verify): 9 raw -> 4 CONFIRMED / 5 REFUTED. Confirmed: [CRITICAL] resetForSignOut destroys unsynced pendingSyncQueue scans (silent permanent loss, dialog promises "counts are saved", regression test enshrined the loss); [IMPORTANT] paidComputeCharged zero direct pipeline-test coverage (route.d4 mocks the pipeline); [IMPORTANT] wrong-PIN rejection path untested; [IMPORTANT] account_daily_cap 429 reasonCode unrecognized client-side (generic retry msg instead of honest cap reason). Refuted (opus, evidence-based): 2 rehydrate-race criticals, setBusinessContext pending-queue-hides claim, charge failure-domain split, resetForSignOut unit-test class dup.
FIX WAVE DISPATCHED (single opus batch, base 039dfc8): F1 prepareSignOut drain+honest-warn guard (action stays full-clear), F2 pipeline paidComputeCharged assertions, F3 wrong-PIN tests, F4 account_daily_cap honest mapping, N1 e2e I1-guard rehydrate strengthening, N2 residue clear + hasLegacyBlob emptiness, FLAKE ai-lookup route mock in p2-accounts spec.
SPEED MODE v2 (owner ask "how can we move faster", 2026-07-19 late): adopted (1) phase-overlap - P3 planning scouts launched DURING P2 fix wave (read-only, no collision); (2) P3-P6 plans must favor net-new modules over scanStore-monolith edits (dependency chains, not agent caps, were the P2 bottleneck); (3) reviews trail the commit line - per-track batch reviews default, dedicated same-turn review ONLY for security-critical tasks, ultra review stays; (4) continuous background gates per commit; (5) controller transcribes trivial verbatim files directly. OPEN OWNER QUESTION (non-blocking): P3 tire-wedge (DOT date-code) keep/cut.
P3 SCOUTS DISPATCHED (3 sonnet, read-only): sessions surface -> p3-scout-sessions.md, cross-device sync surface -> p3-scout-sync.md, report+locations surface -> p3-scout-report.md. Running alongside the P2 fix wave (4/5 agents).
OWNER DECISION: P3 tire wedge (DOT date-code) CUT - 'do not do anything with DOTs we dont scan those'. Master plan edited in place; memory tire-wedge-dot-cut written. P3 plan will contain zero DOT scope.
FIX WAVE COMMITTED (all 7 items; fixer gates: 91 focused + store 390/390 + p2-accounts 6/6 twice + tsc clean). Foreign tools/fable5 commits noted on branch (separate workstream, zero src/e2e overlap). Final 4-shell gate fan-out launched.
P3 scout-sessions DONE: getScanEventsBySession DOES NOT EXIST (master plan assumed it - net-new query path needed); zero device/window identity infra; REAL BUG found: listSessions/reopenSession call getMockDb() directly bypassing the sync target (broken for cloud accounts - P3 fix); traps: finishSession never clears sessionId, reopenSession wipes scanFeed (timeline must read ScanEvent rows), ledger invariant is per-session, pinned test clock. Report: p3-scout-sessions.md
=== PHASE 2 COMPLETE (2026-07-19 ~23:00) ===
FINAL GATES at 9672732: unit 2573/0/34 (268+ files); tsc clean; lint 85 = exact pre-existing baseline; build SUCCESS; test:ledger 41/41; test:firebase green; e2e 49/1 (the 1 = goupc-ladder cap-timing, pre-existing since 9226f3d, same exception as P1 close). All 17 tasks + ultra-review fix wave (7 items incl. the sign-out data-loss CRITICAL) landed. P2 was built DIRECTLY on feat/decode-ladder-goupc - no merge step needed; branch never pushed. Foreign tools/fable5 commits coexist on branch (separate workstream, zero overlap).
Process caught 6 real defects this phase beyond plan bugs: gitignore auth swallow, 2 vitest include gaps, I1 coalesced-key resurrection, e2e migrate partial-blob clobber, sign-out unsynced-scan destruction (CRITICAL, ultra review).
NEXT: P3 plan-writing on scout reports (sessions DONE w/ real bug found; sync + report scouts in flight). DOT tire wedge CUT by owner. /code-review ultra moment FLAGGED to owner at this close.
P3 scout-report DONE: NO price/cost data exists anywhere (Shop-Ware adapter deliberately strips it; UPCItemDB price discarded) - Boss Report inventory value needs net-new price input, flag for plan panel; moat line computable today from ScanEvent statuses (zero schema change); print CSS + share link fully greenfield (no public-read precedent, P2 auth constrains tokenized link; Turso KV = closest server store pattern); countSnapshots ring buffer = recents pattern to copy; ScanEvent/InventoryCount carry NO location field. Report: p3-scout-report.md
OWNER ORDER (token-lean mode, 2026-07-19 ~23:10, supersedes prior panel/review sizing where they conflict): session tokens near cap - burn the least tokens possible. New defaults: (1) shells do ALL verification (tests/greps/gates = zero model tokens); (2) P3 plan = ONE sonnet writer + ONE sonnet adversarial reviewer + controller verification (no 3-agent panel, no opus unless security-critical); (3) implementers haiku for verbatim / sonnet for judgment, terse reports; (4) reviews = controller diff-verification default, single model review only for security-critical tasks; (5) phase-end ultra review SLIMMED (2 finders max, verify only criticals - no 14-agent Workflow); (6) Fable output lean; (7) defer nonessential agent work.
P3 scout-sync DONE: outbound solid (txn+_appliedKeys, additive counts, emulator-pinned); ZERO inbound (no listeners), zero device identity, loadBusinessData one-shot at login only; LAW constraint: inbound must MERGE never replace. P3 PLAN WRITER dispatched (ONE sonnet, token-lean pipeline; plan -> docs/superpowers/plans/2026-07-19-phase3-sessions-sync-report.md). Next: one sonnet adversarial review + controller shell verification.
OWNER ORDER (2026-07-20 ~00:55): continue phases; CODEX EXECUTES implementation tasks (via codex:codex-rescue, billed to ChatGPT plan), ORCHESTRATOR REVIEWS - Fable verifies every Codex diff against the brief, runs all gates in shells, serializes commits. Claude subagents near zero. Codex does NOT commit. P3 plan review currently running on Codex.
P3 PLAN REVIEW recovered: the sonnet reviewer COMPLETED p3-plan-review.md (16KB) before the session cap ate only its summary message; Codex re-run was redundant (killed, no thread). Verdict NEEDS-EDITS: C1 cloud sessions computed-then-discarded (no sessions store field - AC1/AC3 structurally unmet on cloud), C2 location/recentLocations never persisted (scanPersist base omits them - AC4 breaks on reload), I1 location missing from CUSTOMER_SAFE_SCANEVENT_FIELDS, I2 share-mint route prose/handler auth contradiction (unauthenticated unbounded write), I3 folds into C1, I4 mandate the cloud-test grep, M1-M5 minor. Anchors overwhelmingly accurate; DOT clean; dashes clean; law/tenancy/spend/test-safety GOOD. CODEX DISPATCHED to patch the plan per prescribed fixes; orchestrator verifies by grep, then commits + starts Codex execution with Task 1.
P3 EXECUTION START: plan committed 42fd07e (3,022 lines, 15 tasks). Task 1+2 base = 42fd07e. Codex executes, orchestrator reviews.
P3 Task 1: complete (Codex impl, RED->GREEN 5/5; orchestrator review: verbatim vs brief, APPROVED; committed).
P3 Task 2: complete (Codex retry, 7/7; orchestrator shell-diff review: verbatim minus filename comment; committed).
P3 Task 3: complete (Codex impl faithful incl. mandatory grep reconciliation; ORCHESTRATOR GATE CAUGHT: get().sessions forward-reference to Task 7's field - tsc error invisible to vitest; controller declared+initialized sessions:[] at first-reader; gates: focused 16/16, ledger 41/41, store suite 395/395, tsc clean; committed). CARRIED TO ULTRA REVIEW: session rotation (startSession precedent + ensureAutoSession) wipes pendingSyncQueue - same unsynced-loss class as P2's F1 sign-out fix; evaluate drain-first guard. Completed-session processScan guard returns null = plan-mandated, mirrors sanctioned locked-session pattern; scan page calls ensureAutoSession first (backstop only).
P3 Task 4: complete (Codex thread edits verbatim vs brief despite failed resume wrapper - orchestrator recovered by direct verification; ledger gate 43/43 incl. 2 new cases, tsc clean; committed).
CODEX OPS NOTE: companion task sandbox defaults READ-ONLY; --write flag required for implementation tasks (root-caused from companion source :491 'sandbox: request.write ? workspace-write : read-only'). All future dispatches mandate --write explicitly. Task 5 re-dispatched with it.
P3 Task 5: complete (Codex --write, dead-job recovery: 8h-stale job, edits complete on disk, orchestrator self-gated; Codex caught+fixed a test-safety hole in brief e2e snippet unmocked Turso touch; orchestrator fixed PersistableScanState fixture tsc fallout; gates: moat+persist 9/9, store 398/398, e2e 4/4 scanner-focus intact, tsc clean; committed). Task 11 (Boss Report, disjoint) unblocked now moatStats committed.
CONTROLLER ERROR + FIX: Task 11 brief was mis-cut (grabbed Task 12's line range 2336-2776 during the Task 5 commit's re-cut). Codex correctly REFUSED the conflicting brief rather than implementing the wrong task - good executor discipline. Re-cut at correct range 1952-2335 (bossReport + report page), re-dispatched. Lesson: verify head -1 of every re-cut brief matches the intended task heading BEFORE dispatch. Task 6 running untouched in parallel.
P3 Task 6: complete (Codex --write; additive read path, write untouched; mockDb 8/8; emulator BLOCKED in Codex by PowerShell npm.ps1 policy - ORCHESTRATOR ran it via Bash: test:firebase 48/48 incl. new read test; committed). Note: run test:firebase via Bash not PowerShell (npm.ps1 execution-policy block).
P3 Task 11: complete (Codex --write retry after brief-cut fix; pure aggregator, honest null value/hasAnyCostData:false, moat over scanFeed, embedded print CSS; 4/4, tsc clean, no dashes; deviation currentSnapshotForVariance accepted as safe superset; committed). 8/15 done.
P3 Task 7: complete (Codex --write; LAW-CRITICAL inbound merge, orchestrator source-verified all 4 invariants + the pending-guard is doubly-robust in production - Codex deviation strengthened it; 4/4, store 402/402, ledger 43/43, tsc clean; committed). 9/15 done.
P3 Task 8: complete (Codex --write; two-device concurrent-scan test + Refresh button w/ focus guard; emulator BLOCKED in Codex by firebase-tools config EPERM - orchestrator ran via Bash: test:firebase 49/49 incl. new two-instance test; committed). 10/15. Task 9 (processScan stamping) still in flight - its mid-write scanStore state is the transient tsc error, not a Task 8 defect.
P3 Task 9: complete (Codex --write; location/deviceId stamping, deviceId excluded from customer-safe; ORCHESTRATOR fixed generic-helper tsc excess-property error Codex's vitest missed; scanLocation 3/3, persist 7/7, ledger 43/43, store 406/406, tsc clean; committed). 11/15.
P3 Task 13: complete (Codex --write; unitCost manual + CSV import, platform-only confirmed out of AI+customer paths; ORCHESTRATOR relocated the field from a declare-module augmentation in scanStore to types.ts proper - scope tension resolved cleanly; import+table 22/22, tsc clean; committed). 13/15. Task 12 (share link) still in flight.
P3 Task 12: complete (Codex --write; share link, orchestrator hand-audited security: auth-gated mint 401/403-before-mint, 32KB/413, UUID token, immutable-snapshot resolve no tenant query, public page outside AuthGuard; store 3/3, tsc clean, build green; committed). 14/15.
OWNER ORDER (2026-07-20, faster v3): deploy MORE sub-agents + MORE shells at once; add SONNET implementer executors ALONGSIDE Codex (both execute disjoint tasks in parallel), quality unchanged. New default: fan executors as wide as disjoint-file sets allow (Codex + up to several Sonnet workers concurrently), gate/commit as each lands, overlap next-phase scouts with current-phase tail. Orchestrator still reviews every diff + runs shell gates.
P3 SWEEP (Task 15) at 351240f: unit exit0 (full suite), ledger 43/43, golden exit0, firebase 51/51 (both new P3 emulator tests), tsc clean, build green (all new routes), lint 86 = baseline 40 errors flat +1 warning in scripts/ (NOT phase-3); e2e running. Task 14 visual: formal visual-polish agent pass deferred (session-timeline/boss-report e2e specs were out of Tasks 10/11 scope) - lightweight markup self-review clean (no TODO/raw styles, print CSS present, tailwind-styled).
P3 ALL 15 TASKS + gate-sweep regression fix + checkpoint COMMITTED (9785a81..118c9b8). Gates green (e2e 51/1 pre-existing). Task 14 formal visual-polish deferred (honest). SLIM ULTRA REVIEW next (token-lean: 2 finders tenancy/law + ledger/spend, verify criticals only). Then P3 close + P4 planning (p4-scout-import.md already landed - Codex+Sonnet parallel executors per speed-v3).
OWNER ORDER (2026-07-20): keep going through ALL phases; use CODEX for execution AND REVIEW (per-task reviews, adversarial verification of ultra findings, second opinions) alongside Sonnet. Applied: ultra-review criticals will be Codex-adversarially-verified before fix.
P3 SLIM ULTRA REVIEW (2 sonnet finders): 2 real findings beyond the sweep-caught regression.
- CRITICAL (A): ensureAutoSession stale-DEVICE-OWNED rotation wipes pendingSyncQueue -> unsynced offline scans lost on remount after 30min (TOP-LEVEL LAW). Adopt-fix 0d7b9fc only covered no-deviceId case. CODEX verify+fix dispatched (preserve pendingSyncQueue+syncedScanEventIds across rotation + drain; RED test seeds pending before rollover).
- HIGH (B): report/page.tsx handleShare POSTs only {sessionId}, never reportSnapshot/businessId/idToken -> empty "0 items" link (mock) or 401 (live); share ROUTE is correct, PAGE caller is broken; unit tests built full requests so missed it. CODEX fix dispatched (build snapshot client-side from store + send idToken in live).
- LOW (B): top-variances never wired into buildBossReport call (dead sub-feature) - fold into the HIGH fix (same file).
Both finders confirmed clean: scanner-focus law, refreshFromCloud guard non-vacuous, adopt test genuine, no new paid/AI, unitCost out of AI+customer paths, no dashes, server/client separation correct.
=== PHASE 3 COMPLETE (2026-07-20) ===
All 15 tasks + gate-sweep adopt-fix + 2 ultra-review fixes committed (9785a81..2494518) on feat/decode-ladder-goupc (NEVER pushed). Codex-executes/orchestrator-reviews split throughout; Codex also adversarially-verified the 2 ultra criticals per owner order. Ultra review (2 sonnet finders) found CRITICAL (unsynced-scan loss on stale rotation) + HIGH (broken share button) + LOW (unwired variances) - all Codex-fixed with failing-first tests, orchestrator-gated. Final gates green (ledger 43/43, tsc clean; full sweep was 51/1 e2e pre-existing goupc). Push/deploy/code-review-ultra owner-gated.
NEXT: P4 (Universal Import) - both scouts done (p4-scout-import.md, p4-scout-matcher.md), Codex plan writer RUNNING. Then review -> execute w/ Codex+Sonnet parallel.
OWNER ORDER (2026-07-20): OPUS AUTHORS ALL PLANS (orchestrator builds plans itself, no subagent writer); CODEX REVIEWS the plan + is MAIN EXECUTOR; Sonnet executors most tasks, Opus for select high tasks; max parallelism, combine all, never lose quality. Recorded in orchestrator-operating-contract #13.
P4 PLAN: Codex writer produced EXCELLENT complete Global Constraints (all landmines ruled: canonical jaccard, fuzzy-never-approve, sanitizer boundary closed, Turso KV mapping-memory, persist v9 migrate, honest BIFF-.xls reject, preview-before-apply, 10s/5000-row perf) but only 5 of 15 task BODIES (truncated at Task 5). Header defines 15: T1-11 Stage A, T12-14 Stage B, T15 gate. Opus (orchestrator) now taking over authorship of Tasks 6-15 per new order; Codex-writer cancelled. Then Codex reviews.
OWNER DECISIONS (2026-07-20, via AskUserQuestion): (1) codify the Opus-plans/Codex-reviews+executes model as a REUSABLE SKILL + global doctrine (a /big-plan command, all projects); (2) SIZE-GATE it (full machine only for substantive multi-task phases; solo-Opus for small tasks); (3) BRIEF COST CHECK-IN per phase before each execution wave (rough token/spend + agent count, non-blocking); (4) Opus authors P4 Tasks 6-15 NOW. Deferred build: the /big-plan skill + doctrine update (do after P4 momentum, or when owner wants).
P4 PLAN COMMITTED: 11 Stage-A tasks. Opus authored T11 + reorder + constraint reconcile (Codex writer runaway killed, reordered its 6-10 prepend, Stage B -> P4b follow-up). Next: Codex adversarial review of the plan, then execute.
OWNER SPEED CONFIG (2026-07-20, "move way faster", via AskUserQuestion): ALL levers ON - (1) MAX CONCURRENCY: run every disjoint-file task at once, 8+ agents (Codex + multi-Sonnet + Opus), serialize only true file conflicts; (2) LIGHT-TOUCH REVIEW: orchestrator diff-verify + shell gates default, model review ONLY for security/monolith(scanStore)/law-critical tasks, end-of-phase ultra is backstop; (3) BATCH GATES per track (not per commit); (4) OVERLAP next-phase planning during execution. Review floor = diff-verify + deep-review-risky-only. COST CHECK-INS DROPPED mid-run - report spend at phase close only.
P4 base commit: ab6b0c4
P4: T1(0e050ab) T5(eca6878) T2 committed; T6 running, T3 running, plan-review running. T4 unblocks when T3 lands.
P4 PLAN REVIEW (sonnet fallback, Codex over quota): verdict REJECT, 7 criticals - but reviewer CONFABULATED a nonexistent "existing review file" and got I6 wrong (import-apply DOES exist Task 9:272). ORCHESTRATOR verified every claim vs source: C1 REAL+CRITICAL (statusForMatch:1564 returned 'exact' for header-mapped UNMATCHED -> Task 10 autocounts = forbidden wrong-identity autocount) FIXED in plan; C5 REAL (Task 11 wrong import module + sheet.matrix) FIXED; I6 FALSE; C6/C7 acceptable (unit perf guard + core e2e; Boss-Report leg covered by P3 tests); C2 infra-DDL not a count-law write; C3+C4 (imported-review keeps live decode; duplicate exact rows must SUM importQuantity not overwrite) -> FIX IN TASK 10 BRIEF before dispatch. Lesson: fallback reviewer partly hallucinated but surfaced a genuine top-failure critical; verify-don't-trust caught both the real bug and the false one.
P4: T1-6 committed (7/11); T7(classifier+C1fix)+T8(retail) running. Briefs 9/10/11 pre-cut. T10 brief needs C3(imported-review no live-decode)+C4(duplicate exact rows SUM importQuantity) patched before dispatch. Codex down->Sonnet main executor per owner; fold Codex back on credit return.
P4 CODEX PLAN REVIEW (completed - didn't die on quota; wrote real p4-plan-review.md): REJECT 8C/7I/3M. ADJUDICATED vs source: C2(wrong-id autocount)=ALREADY FIXED (statusForMatch, confirmed in Task7 code+test); I2(sub-0.75 confidence fixture)=executor FIXED in Task7; C1+M1(15-task doc contradiction)=FIXED (map+constraints reconciled to 11 tasks); I5(barcode validation)=FIXED; 7/8 tsc coupling (PreviewMatchResult)=FIXED+committed 0f848c2+this. STILL OPEN (fix before dispatch): C4(imported reviews keep liveDecode/correctionRecheck -> /api/ai-lookup) + C5(duplicate exact rows overwrite not sum importQuantity; promised resolveImportReview absent) = TASK 10 brief; C6+C7+C8(Task 11 fixture contract violations: sheet.rows vs headers, tireSize vs size, validateManualMapping sig, file.text() stub, base64 xlsx untested; AC4 5000-row not timed end-to-end; AC5 variance/report + 380-of-400 not proved) = TASK 11 REWRITE. Acceptable/noted: C3(mapping GET ensureTables DDL=idempotent infra not data-write), I3/I4(coverage gaps), I6(BIFF .xls owner decision), I7(remembered-reimport proof - fold into Task11).

=== P4 RESUME CHECKPOINT (2026-07-20, context ~31%) ===
COMMITTED: P4 Stage A Tasks 1-9 (importSchema, sanitizer+D9, columnIntelligence, universalFileReader, importMappingMemory, import-mapping route, universalImportPreview classifier w/ C2 fix, reconcile/match retail enrichment, UniversalImportPanel). Plan docs reconciled to real 11-task structure. Branch feat/decode-ladder-goupc, NOT pushed. Codex OVER QUOTA (Jul 26 / credits) -> Sonnet is main executor.
DONE+COMMITTED: Task 10 (C4+C5 both fixed+tested; full 2678/0, ledger 43/43, tsc clean).
NEXT AFTER TASK 10 lands:
 1. Review scanStore diff CAREFULLY (monolith/law-critical): verify C4 (no live-AI action on import reviews) + C5 (dup cleanCode sums qty) + persist v9 keeps v8 no-inject rule + ledger invariant. Run test:ledger + tsc via Bash. Commit.
 2. Task 11 = REWRITE for review C6/C7/C8. Dispatch Sonnet to rewrite p4-task-11 test files against REAL contracts: inference matrix = [sheet.headers, ...sheet.rows]; signature = hash sheet.headers; ColumnMapping key is `size` NOT `tireSize`; validateManualMapping(headers, mapping); file stub needs BOTH text() and arrayBuffer(); decode+test the base64 xlsx fixture. AC4: time end-to-end readUniversalFile+infer+buildImportPreview on 5000 rows < 10s (not just inferColumnMapping). AC5: 400-row fixture, assert exact headline "Matched 380 of 400 automatically", navigate e2e to variance + /report and assert report data. I7: prove remembered-mapping on re-import (PUT then GET then 2nd import).
 3. Full batched gate sweep (npm test / test:ledger / test:golden / test:firebase / tsc / lint / build / test:e2e - all via BASH not PowerShell). Fix root causes.
 4. PROGRESS.md Stage A checkpoint (Task 11 step 6): AC1/AC3/AC4/AC5 mapping, known limits (BIFF .xls owner decision I6; ladder_kv reuse; C3 mapping-DDL infra).
 5. Slim ultra review (2 sonnet finders: tenancy/law + regression/test-integrity) over the P4 Stage A whole-phase diff. Codex-verify criticals when Codex returns.
 6. Local merge stays owner-gated (never pushed). P4b Stage B fuzzy = separate plan authored later. Restore Codex as main executor + reviewer when quota returns.
ADJUDICATED-CLOSED review items: C1/M1(docs), C2(statusForMatch), I2(confidence fixture), I5(barcode validation), 7/8 tsc coupling. OPEN-in-task: C4/C5(Task10 - in dispatch), C6/C7/C8/I7(Task11 rewrite). Noted-acceptable: C3, I3, I4, I6.
RESUME POINT: 10/11 committed. NEXT = Task 11 REWRITE (C6/C7/C8/I7 - see spec above) then batched sweep + slim ultra review. Codex still down (Sonnet fallback).
ARGUS wired into review layer (owner order 2026-07-20): local Python reviewer `python -m tools.fable5` - review-plan (structural+proof audit) + review-build (semgrep/invariants/mutation/risk arsenal) + doctor/selftest. DETERMINISTIC, shell-run, zero model tokens - standing layer alongside orchestrator diff-verify + model semantic review. Ran review-plan on P4 plan: structural nits (no Risks/Rollback/Cost HEADINGS though info is in Global Constraints) + false-positive on scanStore.ts:line anchors read as missing files. Codified in BIG_PLAN_EXECUTION_DOCTRINE. ADD to phase-gate steps: run `python -m tools.fable5 review-build` alongside the shell test battery.

=== P4 STAGE A ULTRA REVIEW + SECURITY FIX (2026-07-20) ===
Production-surface phase gate GREEN (verified from real output, not tail exit): unit 2678/0/36skip, tsc clean, ledger 43/43, golden 2/2, firebase 52/52, build SUCCESS (all P4 routes), lint 40-err flat baseline, ARGUS review-build PASS blockers=0.
ULTRA REVIEW ran 2 core finders (law/tenancy/security + regression/ledger/test-integrity) + 7 breadth Sonnet lenses (security-2nd, data-integrity, copy, a11y, design, simplicity, architecture), all read-only on committed ab6b0c4..23e3465.
Core finders: C1/C2 wrong-identity, C4 no-live-AI-on-import, C5 dup-sum, sanitizer, persist-v9-non-injective, ledger-integrity, test-integrity ALL confirmed CLOSED/clean. 3 Minors (unpaired audit snapshot on unreachable throw; dedup-key raw-not-normalized; PreviewMatchResult dup).
ONE Critical found+FIXED+COMMITTED a313917: import-mapping + share routes used raw process.env.IS_E2E read (not NODE_ENV-scoped) -> swapped to hardened isAuthBypassEnabled(); closes known-open P3 F5. Route tests 13/13 green.
BACKLOG for owner (NOT applied - owner wants to direct polish): SEC-HIGH idToken-in-query-string (container GET -> logs); DATA-HIGH blank-qty->0 silent-loss + aggregation-key-collision misattribution; DATA-MED no qty upper bound (browser-hang footgun); COPY jargon leaks + informational-styled-as-error + apply/saveMapping success-shown-as-error; A11Y 3 aria-live; DESIGN green-shade split + button-convention drift; ARCH dup PreviewMatchResult + loadMapping swallow; SIMPLICITY 2 dead fields; SEC-LOW xlsx cell cap + reconcile byte cap.
Task 11 (ship-gate rewrite) implementer running (Sonnet, against verified contracts). AFTER it lands: verify diff, run unit+e2e, commit Task 11, PROGRESS.md checkpoint, then STOP for owner review (owner order: do NOT start P5).

=== P4 STAGE A COMPLETE (2026-07-20) ===
Commits on feat/decode-ladder-goupc (NOT pushed): a313917 auth-bypass hardening (import-mapping+share -> isAuthBypassEnabled, closes P3 F5); 067d7d2 phase3 moat fix (ensureAutoSession ADOPT branch renamed 'Default Session' placeholder -> buildAutoSessionName; phase3-location-moat e2e now 3/3); f50b5e4 remove superseded CsvImportPanel (dead in prod, obsolete e2e deleted); e6c83ec Task 11 ship gate (4 fixtures + battery + 5000-row perf + mapping-memory roundtrip + 2-viewport e2e 'Matched 1 of 2 automatically' real corpus PN) + ultra-review polish (a11y aria-live, loadMapping throw-on-!ok, dead-field removal, PreviewMatchResult dedup).
GATES at commit time: unit 2682/0, tsc clean, ledger 43/43, golden 2/2, firebase 52/52, build SUCCESS, lint 40-err flat baseline, Argus review-build PASS blockers=0, route tests 13/13. FINAL SWEEP (unit/e2e/ledger/argus) running.
ULTRA REVIEW (2 core + 7 breadth + 3-HIGH adversarial verifier): 1 Critical FIXED (auth-bypass). 3 HIGHs CONFIRMED by verifier, DEFERRED to backlog (owner may direct): (H1 idToken-in-query-string GET -> logs, likelihood low-med, fix=Authorization header; H2 blank-qty cell Number('')=0 silent-loss, likelihood HIGH, fix=reject empty-string before coerce; H3 aggregation key raw-text not resolved-identity -> quantity misattribution on colliding barcode/PN/name, likelihood med, fix=key on match identity + divergence guard). Plus copy jargon-leaks, design green-shade/button drift, sec-low xlsx/reconcile caps. Detail: p4-high-findings-verification.md, p4-quickwin-patches.md (unused items).
GIT HYGIENE NOTE: working tree has PRE-EXISTING uncommitted changes unrelated to P4 (docs-overhaul: CLAUDE.md/AGENTS.md/README.md/docs/ARCHITECTURE.md/COMMANDS.md/PLAN_EXECUTION.md/docs/decode/*; untracked share route.test files; tools/__init__.py; the 2026-07-20 plan file working-tree delta). NOT bundled into P4 commits - left for owner.

=== P4b + P5 FULLY SCOUTED (2026-07-20, ready to author) ===
OWNER ORDER (2026-07-20): proceed through ALL remaining phases; sustain >=7-10 subagents + multiple shells concurrently at all times; thoroughness confirmed good (caught csv-import regression + moat regression + wrong-identity autocount pre-ship). Codex still down -> Sonnet main executor.
P4b Stage B (fuzzy matching) scouts: p4b-scout-matcher.md + p4b-scout-fixtures.md. Reuse identityMerge.ts jaccard/nameTokens/IDENTITY_JACCARD_THRESHOLD(0.75, canonical); net-new levenshtein/edit-distance (none exists); plug into identityMatcher.ts Step 2 (line 202-240) AFTER exact size-token + brandsCorroborate gates (size stays EXACT, hard gate); guardrail STRUCTURAL (statusForMatch routes non-part_number_exact matched -> fuzzy -> review, 0 auto-merge below T already enforced); ImportPreview.{total,exact,fuzzy,review,reject} for AC2 count assertions; adversarial near-duplicate-BRAND fixture is net-new (brandFamilies.ts seeds it). Stage B files not built (clean slate).
P5 Decode Trust Round scouts: p5-scout-gpt-demotion.md, p5-scout-crosstier.md, p5-scout-pland.md, p5-scout-golden.md.
 - D6 GPT demotion: gptLadderRung.ts:126-137 gptResultToDecodePayload hand-builds status:'verified' w/ evidenceStrength:'none' (never calls decideDecode); scanGates.ts:93-99 canAutoCount 'gptTrusted' branch auto-counts it -> the mechanism to demote to 'suggested'. Go-UPC WORSE: GoUpcProvider.ts:149-166 hardcodes fetched_source + exactCodeEvidenceVerifiedByApp:true for raw API JSON. Badges (badges.tsx) need app-confirmed-vs-AI split. catalogEntries master-truth append = NEW work (only a constant + tiered stub exist). 7 unit test files assert today's wrong behavior.
 - D5 cross-tier conflict: resolver is FIRST-MATCH-WINS (aliasMatcher.ts:146-159 short-circuits alias-before-identifier, then primary_barcode>sku>gtin>upc>ean); pickTier(48-67) only catches SAME-tier dup; resolveScanToProductTiered (166-182) is a NO-OP pass-through (void masterCandidates) w/ ZERO callers = D5's landing spot; scanStore:1790 still calls untiered path. padded-GTIN equivalence MISSING in resolver (gtin.ts:34-54 canonicalGtin/gtinVariants exists, unused there); tenant-vs-master unplumbed (CatalogEntry lacks productId/provenanceTier). Zero D5-matrix tests.
 - D8 Plan D: pipeline.ts:1149 wires Plan D lookupBarcodeDb -> barcodeDbProvider.ts:22 which RE-FETCHES upcitemdb (already queried rung-0 pipeline.ts:890) w/ no usage tracking; retailDb(1150) correctly REUSES retailHit closure = the fix pattern to mirror. Go-UPC evidence relabel point GoUpcProvider.ts:154. No once-per-request spy test; stale excuse comment pipeline.test.ts:829-832. L12 $-cap is fine; gap is upcitemdb provider quota double-fetch.
 - Golden gates: goldenBaseline.test.ts (84/100 corpus via lookupByExactBarcode); goldenClasses.store.test.ts (4 AI-off classes, in test:ledger); REAL gate = scanGates.ts:83-107 canAutoCount; eval.test.ts has falseAutoCountRatePct===0 invariant but runEval.ts:44-63 HAND-MIRRORS gate logic instead of calling canAutoCount (drift gap to close); expand eval dataset per-class + suggested-precision-floor; qa:revision never runs vitest (CI gap). Must-not-regress: 84 golden codes, 4 classes, 14+ ledger invariants, non-gpt canAutoCount branches (AC5).
RESUME POINT: P4 Stage A COMPLETE + committed. NEXT = author P4b plan (Opus) from the 2 p4b-scout files -> review -> execute; then P5 from the 4 p5-scout files; then P6. Sustain 7-10 agents. Consider /compact before the P4b->P5->P6 authoring marathon if context tight.

=== P4 STAGE A FINAL GATES (2026-07-20) - ALL GREEN ===
unit 2682/0/36skip; ledger 43/43; tsc clean; golden 2/2; firebase 52/52; build SUCCESS; lint 40-err flat baseline; e2e 53 passed / 1 failed (ONLY the known pre-existing goupc-ladder.spec cap-timing flake, P4 touched zero decode code, same exception as P1/P2/P3 closes); Argus code-checks ALL PASS (semgrep/proof-local/invariants/gitleaks/no-em-dash/key-safety/ledger-law/server-import-boundary) - Argus overall verdict was BLOCK but blockers=1 = its OWN flaky self-test test_personas.py:182 (order-nondeterministic persona dispatch, same multiset reordered; Argus-engine bug not Scanbin code; logged to Argus backlog). Stray untracked tools/__init__.py (41B docstring, not mine) left in place.
P4 STAGE A DONE. 4 commits: a313917 067d7d2 f50b5e4 e6c83ec. Branch feat/decode-ladder-goupc, NOT pushed.

=== GOUPC HONEST-REASON FIX WAVE (2026-07-20, owner-directed) ===
Owner saw the goupc-ladder e2e "failure" (the long-standing "flake") and demanded a real fix. systematic-debugging agent proved it a REAL PRODUCTION BUG + honest-reasons-law violation (deterministic, 2x byte-identical): sanitizeCustomerReason (decodeFallback.ts) collapses EVERY all-miss reason to generic boilerplate because the all-miss string lists rung names (denylisted) and pipeline.ts:1402 passes no ctx; HONEST_CONTEXT_REASON["cap_blocked"] is dead (decision.status never = cap_blocked). Root-cause: .superpowers/sdd/goupc-cap-rootcause.md. Owner picked ALL 4 fix options via AskUserQuestion + 2 additions: (5) REMOVE Go-UPC monthly cap entirely (owner subscription, utilize fully); (6) run as a parallel sub-agent ribbon.
KEY DESIGN RECONCILIATION: the e2e asserted the CUSTOMER feed shows "Go-UPC monthly cap reached" - but "Go-UPC" is a denylisted vendor token under BUG #14 anti-leak law. Correct fix = honest TOKEN-FREE reason ("a lookup service is at its usage limit"), and the e2e assertion CHANGES to that + asserts NOT contains "Go-UPC". Reconciles honest-reasons law + anti-leak law.
3 DISJOINT-FILE implementers dispatched (Sonnet, TDD, no-commit): A=honest all-miss reason [decodeFallback.ts allMissReasonCode+MISS_REASON_TEXT token-free, pipeline.ts:1401 wiring, kill dead cap_blocked, decodeFallback.test.ts, e2e goupc mock+assertion] ; B=Go-UPC no-limit [goUpcUsage.ts resolveLimit->Infinity default, configurable-reversible lever, keep record() for observability, keep daily-AI cap untouched, GoUpcProvider gate, goUpcUsage.test.ts] ; C=visibility [scanStore.ts scanFeed decodeNote refresh-after-settle (kills stale "Decoding with AI..."), LiveScanFeed.tsx render honest reason, tests]. 3 = honest disjoint max (more would collide on pipeline.ts/scanStore.ts).
COST CAVEAT (owner Paid-API-Cost-Truth rule): Go-UPC no-limit is LOCAL code only, no live calls; confirm Go-UPC billing console the plan is truly flat/unlimited (not N-included-then-overage) before heavy prod use; daily-AI cap stays as cross-rung backstop.
NEXT: collect 3 reports -> orchestrator diff-verify (A law-critical = careful review) -> gates (unit+e2e+ledger+argus) -> commit -> then back to P4b/P5/P6 marathon. Effort=HIGH now.

=== GOUPC FIX WAVE COMMITTED + P4 BACKLOG WAVE (2026-07-20) ===
Session RESTARTED mid-gate; working tree survived (verified HEAD + all files). GOUPC honest-reason fix wave COMMITTED: 787c6b9 (A honest token-free all-miss reasons + goupc e2e assert not-contains Go-UPC), 1e6ab31 (B Go-UPC unlimited-by-default + JSON-safe status limit:null/unlimited:true; daily-AI cap untouched), ccb1996 (C decodeNote refresh-after-settle). Combined gates GREEN: unit 2713/0, tsc clean, ledger 43/43, route 46/46, e2e 54/0 (goupc-ladder cap test now PASSES - the standing "flake" was this real bug, now fixed). Argus 2 blockers both adjudicated NON-defects: python-self-tests (known persona-ordering engine flake) + proof-local (scanFocus.test.tsx load-timing flake, proven 3/3 isolated). The 3/53 e2e cliff during the restart = stale prior-run webserver contending for port 3100 (clean re-run = 54/0).
P4 BACKLOG HIGHs now being fixed (3 disjoint parallel agents, owner "different tasks in parallel"): H1 idToken query-string -> Authorization header (import-mapping GET + container + route.test) RUNNING; H2 DONE (blank/whitespace qty rejects not silent-0; MAX_IMPORT_QUANTITY=100000 upper bound; universalImportPreview.ts+test 8/8); H3 aggregation-key-collision -> divergent-identity routes to review not silent-merge (scanStore applyUniversalImport + universalImport.store.test) RUNNING. Each TDD/no-commit; orchestrator gates+commits.
NEXT after backlog: combined gate + commit H1/H2/H3 -> then P4b Stage B plan authoring (scouts done) -> P5 -> P6. Consider /compact before the authoring marathon (deep context).

=== GOUPC FIX + P4 BACKLOG HIGHs ALL COMMITTED + GREEN (2026-07-20) ===
6 commits on feat/decode-ladder-goupc (NOT pushed): 787c6b9 1e6ab31 ccb1996 (goupc honest-reason wave) + 41cfabd (H1 idToken->Authorization header) + 9436b78 (H2 blank/absurd qty reject, MAX_IMPORT_QUANTITY=100000 per-SKU-row, owner-confirmed KEEP) + 96deddb (H3 divergent-identity import collision -> review not silent-merge). ALL 3 P4 ultra-review HIGHs now FIXED (were backlog).
FINAL COMBINED GATES: unit 2724/0 (36 skip), tsc clean, ledger 43/43, store suite 424/424; e2e last clean run 54/0 (goupc-ladder cap test now passes); e2e final re-run in flight. Each fix TDD + orchestrator diff-verified + committed by explicit path (goupc files disjoint from H1/H2/H3 files - no interleave). H3 law-critical (wrong-identity) got a careful diff review.
OWNER DECISION (via AskUserQuestion): MAX_IMPORT_QUANTITY stays 100000 per single SKU row (not row/total count; row count separately up to 20000) - guards the mis-mapped-barcode browser-hang footgun, far above real per-SKU on-hand.
NEXT: P4b Stage B (fuzzy matching) authoring - scouts DONE (p4b-scout-matcher.md + p4b-scout-fixtures.md): reuse identityMerge jaccard/threshold 0.75, net-new levenshtein, plug identityMatcher Step 2 after exact size+brand gates, guardrail structural (statusForMatch), adversarial cross-brand fixture net-new. Then P5 (Decode Trust - scouts done) -> P6. Deep context - recommend /compact before the P4b/P5/P6 authoring marathon (all on disk, resume-safe).

=== P4b + P5/P6 RESUME CHECKPOINT (2026-07-20, post-compact) ===
STATE: P1/P2/P3/P4-StageA all COMPLETE + green on feat/decode-ladder-goupc (HEAD 96deddb, NOT pushed).
OWNER ORDER: proceed through ALL remaining phases (P4b -> P5 -> P6), full arsenal, 5-7+ parallel agents,
each self-reviewing before returning; orchestrator diff-verifies + serializes commits. push/deploy/paid still gated.

P4b (Stage B fuzzy import matching) - Tasks 13/14/15 ALREADY AUTHORED in
docs/superpowers/plans/2026-07-20-phase4-universal-import.md (Codex writer's Stage B survived renumber).
REVIEW DONE: 1 Sonnet adversarial reviewer (EDITS-REQUIRED, 2C/2I) + my own type-verify + Argus (all-noise false-positives).
ADJUDICATED + PLAN PATCHED by Opus:
 - C2 sizeless-row -> require expectedSize (hard size gate, mirrors identityMatcher Step 2). ADOPTED.
 - C1 brand edit-distance = 2nd equality notion -> BOUNDED not removed (reviewer's binary fix would break the
   headline "Micheln->Michelin" typo feature): added FUZZY_BRAND_MIN=0.8 named export to identityMerge.ts;
   brand must be sameBrandFamily OR editSim>=0.8 else null; + adversarial "Kelso vs Kelly -> review" test. ADOPTED-BOUNDED.
 - I4 concat order aligned [sizeText,specs,model]; prefixBrandConflict made unconditional veto.
 - I3 Task15 identityMatcher patch flagged MANUAL-PLACEMENT (non-unique hunk anchors).
 - Rewrote the mislabeled plus-gen test to a real R8/R8+ pair; +sizeless test. Task14 now 7 focused tests.
Type shapes VERIFIED tsc-safe: ExpectedInventoryRow (all fields present), CorpusCandidate exported, tireSizeToken(IdentityText).
EXECUTION: Task 13 (normalizedEditDistance.ts, pure) implementer DISPATCHED (sonnet, TDD, no-commit). Base 96deddb.
 Chain is SEQUENTIAL 13->14->15 (edit-distance -> matcher -> integration). Orchestrator commits each after diff-verify.

P5 (Decode Trust) - FULLY SCOUTED (4 scouts) + anchors RE-VERIFIED current at HEAD by freshness agent. Ready to author.
 Anchors: D6 gptLadderRung.ts:126-137 (status verified despite evidenceStrength none) + scanGates.ts:95-99 gptTrusted branch
  + GoUpcProvider.ts:149-166/261 (fake fetched_source) + pipeline.ts:989 (fabricated evidence). D5 aliasMatcher.ts:146-159
  first-match-wins + :166-182 resolveScanToProductTiered no-op stub (ZERO prod callers; scanStore.ts:1790 calls untiered)
  + gtin.ts:34-54 canonicalGtin exists-but-unwired. D8 pipeline.ts:1148-1150 Plan D re-fetches UPCitemdb via
  barcodeDbProvider.ts:12-22 (untracked 2nd hit) vs retailDb closure-reuse pattern to mirror. Golden: runEval.ts:44-63
  hand-mirrors canAutoCount (drift) instead of importing scanGates; eval.test.ts has 0%-false-auto-count skeleton;
  goldenBaseline 84-count + goldenClasses 4 AI-off classes must stay green (AC5 = only gpt_self_report demotes).
 P5 TASK DESIGN (Opus): T1 D6-core (gptLadderRung verified->suggested + scanGates gptTrusted removal + tests);
  T2 provider-honesty (GoUpc verifiedDecision honest + pipeline.ts:989 evidence + D8 UPCitemdb reuse rung-0, once-per-request
  spy test, stale-comment fixes) - single task owns pipeline.ts to avoid conflict; T3 D5 cross-tier (resolveScanToProductTiered
  real collect-all-then-conflict + canonicalGtin equivalence + wire resolver.ts + scanStore.ts:1790; 5-cell matrix tests) -
  scanStore monolith = dedicated review; T4 golden gates (point runEval at real canAutoCount, per-class fixtures,
  suggestedPrecision floor + 0-wrong-auto-verified, AI-on goldenClasses) - AFTER T1; T5 badges (Verified app-confirmed vs
  Suggested AI) - AFTER T1. WAVE1 parallel: T1,T2,T3 (disjoint files). WAVE2 parallel: T4,T5 (after T1).

P6 (Sell-Ready) - SCOUTED (.superpowers/sdd/p6-scout.md). Key real findings: THREE disagreeing corpus counts
 (meta.json 76,173 / coverage_ledger 76,278 / generated.json 78,243 real keys) - drift floor 75,411 hand-set + not in CI;
 rate limiter in-memory per-process (not durable multi-instance); daily AI cap GLOBAL-first can starve tenants (real
 multi-tenant bug); zero error monitoring/APM; zero instrumentation; ensureUserProfile NOT called on password login
 (staleness bug); no onboarding; settings has no account surface; docs/ARCHITECTURE|COMMANDS|PLAN_EXECUTION.md uncommitted;
 README stale (still "Smart Inventory", stale ladder). P6 plan authored after P5.
NEXT: author P5 plan (docs/superpowers/plans/2026-07-20-phase5-decode-trust.md), Codex/Sonnet adversarial review, execute wave1.

--- P4b PROGRESS (2026-07-20) ---
Task 13: complete (commit f5092f0, orchestrator-verified: correct 2-row Levenshtein, symmetric, [0,1], pure; 3/3, tsc clean).
Task 14: complete (commit ec29efc). Implementer BLOCKED-with-discipline: found a REAL plan-fixture defect (tireSizeToken's
 METRIC_SIZE regex requires a "/" so "225-65-17"/"225 65 17" don't parse -> C2 size gate correctly dropped them), refused to
 weaken the guardrail. ORCHESTRATOR root-cause fix (NOT the implementer's slash-ify-fixtures symptom fix, which would abandon
 the master-plan "size-notation normalization" requirement): added normalizeImportSize() - reformats any-separator NNN[sep]NN[sep]NN
 to canonical NNN/NNRNN (same-numbers-different-separator, NOT fuzzy-on-size); sizeOf tries direct then normalized. Delivers the
 size-notation feature AND keeps every guardrail. 7/7 focused, identityMerge+matcher 36/36, tsc clean. identityMerge FUZZY_BRAND_MIN
 export confirmed additive-only (git diff). Plan patched to match.
Task 15 (integration: identityMatcher identity_fuzzy MANUAL-PLACEMENT + route + 3 fixtures + stageB test + universalImportPreview
 regression + e2e): DISPATCHED (sonnet). Base ec29efc. Trust-critical -> dedicated orchestrator review on return.
P5 PLAN AUTHORED: docs/superpowers/plans/2026-07-20-phase5-decode-trust.md (5 tasks, wave1 T1/T2/T3 parallel, wave2 T4/T5 after T1;
 catalogEntries master write+read DEFERRED to P5b). Adversarial reviewer + Argus review-plan DISPATCHED (parallel with Task 15).
 KEY DESIGN CALL: Go-UPC honest relabel (exactCodeEvidenceVerifiedByApp:false) mechanically demotes it verified->suggested, but
 high-conf (0.9) suggestions auto-apply to the counted row -> identity still lands+counts, badged "Suggested (DB)". Honors owner
 "utilize Go-UPC" + the trust law + the prior salmon-as-beer wrong-Verified-via-GoUpc incident. FLAG at phase close for owner visibility.

--- P5 PLAN REVIEW ADJUDICATED + HARDENED (2026-07-20) ---
Sonnet adversarial reviewer (sub-agent-cross-verified): EDITS-REQUIRED, 2C/4I/5m. Argus: heading-noise only (added Risks/Rollback/Cost section). ALL folded into plan by Opus:
 - C1 (VERIFIED at pipeline.ts:1259): demoting Go-UPC verified->suggested breaks the escalation win-selection (goStatus==="verified" ? paid : freeRun) -> paid Go-UPC answer silently discarded. Task 2 Step 3c added: audit ALL status==="verified" Go-UPC gate sites (grep found :1259 win-select, :1289/:1551 ok, :1327 contradiction-guard note, :1572 L2-cache -> demoted suggestions must STILL cache or repeat scans RE-PAY = cost regression). +update pipeline.test.ts:721-736.
 - C2 (VERIFIED at scanStore.ts:4069): 2nd resolveScanToProduct call site (dedup guard) bypasses resolver.ts -> stays exempt from cross-tier conflict. Task 3 now converts BOTH :1796 + :4069.
 - I3: route.test.ts (:287/:437 assert gpt verified) added to Task 1 files.
 - I4: GoUpcProvider.test.ts has 3 tests (not 1) needing the flip; widened Task 2.
 - I5: Task 5 re-scoped monolith-touching (ScanEvent has no provenance field; ~15 decodeStatus sites) -> P5 bounded to badge component + ONE live-feed site, full threading deferred to P6, dedicated review.
 - I6: gtinVariants GTIN-14 safety is incidental (padStart never truncates) -> added invariant regression test in gtin.test.ts.
 - minors: :1790->:1796 anchor fixed; resolver.ts cascade note incl. resolveScanServer security file; sourceUrl/retry-robustness note in Task 2.
Reviewer confirmed SOUND: AC5 fence holds structurally, decideDecode untouched, P5b scoping valid, conflict envelope preserves appear+count.
P5 PLAN = EXECUTION-READY. Wave1 = T1(gptLadderRung+scanGates), T2(pipeline+GoUpc+barcodeDb), T3(aliasMatcher+resolver+scanStore+gtin). Wave2 = T4 golden, T5 badges (after T1).
LAUNCH: P5 T1 dispatched NOW (disjoint from in-flight P4b Task 15); T2/T3 held until Task 15 commits (clean tree for the hard pipeline/monolith tasks).

=== P4b STAGE B COMPLETE (2026-07-20) ===
Task 15: complete (commit 939e812). Implementer caught+fixed 2 REAL plan-diff bugs (no fake-green): (1) rowSizeToken
 couldn't parse dash sizes -> normalizeImportSize fallback (consistent w/ Task 14); (2) empty candidatesForFuzzy downgraded
 honest "unmatched" -> "ambiguous" -> added non-empty guard (exactly the Minor Opus flagged). +fixed e2e button target
 ("Approve suggestion" not "Create new") + window typing. Orchestrator diff-verified identityMatcher.ts: fuzzy fallback placed
 correctly (after identity-jaccard, before non_tire/unmatched), matchBasis additive, statusForMatch untouched (identity_fuzzy
 never "exact"), route candidatesForFuzzy size-keyed brand-unfiltered. 7 files/61 tests pass, tsc clean, e2e green (fuzzy +
 universal-import desktop+phone). P4b Tasks 13(f5092f0)+14(ec29efc)+15(939e812) ALL COMMITTED. Fuzzy = review-only, never auto-count.
NOTE: docs/superpowers/plans/2026-07-20-phase4-universal-import.md has uncommitted Stage-B hardening edits (Opus) - commit at P4b docs close.
P5 EXECUTION WAVE 1 LAUNCHED (3 concurrent): T1 (gpt demotion) running; T2 (provider honesty + status-ripple audit) + T3
 (cross-tier conflict, law-critical monolith) dispatched. Base 939e812. Shared-tree tsc caveat given to each (own-files gate).
 Serialization = orchestrator diff-verify + commit, one at a time. T3 gets dedicated review (scanStore + resolver, law-critical).

--- P5 WAVE-1/2 PROGRESS + SCOUT INTEL (2026-07-20) ---
P5 Task 1 (GPT demotion): COMPLETE, commit 82ab775 (orchestrator diff-verified: verified->suggested, gptTrusted branch deleted, codeType destructure cleaned, AC5 intact; 116/116, tsc clean). Caught 2 extra route.test.ts ripple sites (in-file, fixed).
P5 Task 4 (golden gates): DISPATCHED post-T1 (base 82ab775). Running.
P5 Tasks 2 (provider honesty) + 3 (cross-tier) + P5b scout: still running.
P5 Task 5 (badges): BLOCKED-by-collision on scanStore.ts (T3 owns it) - launches after T3 commits.

SCOUT INTEL LANDED (all on disk, arm P6/P5b/gate):
- p6-corpus-scout.md: ground truth = tireKnowledge.generated.json barcodeIndex = 78,243 keys. meta.json/RAG_KNOWLEDGE_STATUS stale 76,173 (regen together atomically via build-tire-knowledge.mjs). coverage_ledger 76,278 is harvester-owned diff-schema - do NOT force-match. Drift test is a TAUTOLOGY (claims Turso, reads local file; skipIf checks unused creds). test:corpus-drift in NO ci gate. Regen offline/$0. Retail 4M has zero drift test. FORK for plan author: rewrite drift test as local-manifest-floor (cheap) vs Turso-live-count (new scope).
- p6-ops-scout.md: REAL BUGS - (a) tenant-starving cap route.ts:298-372 global-before-per-account (fix=reorder, AI_LOOKUP_ACCOUNT_DAILY_LIMIT exists); (b) ensureUserProfile not called on password login auth.ts:50-57. Rate-limit checkRateLimit in-memory Map aiSpendGuard.ts:33-53; daily cap ALREADY durable (LadderStorage template); NEW: GPT $3/day guard aiSpendGuard.ts:236-348 also file-only. Zero APM (api routes have no console at all). Zero instrumentation (anchors mapped: signup auth.ts:62/:116, firstScan processScan, retention userProfiles.updatedAt). Empty state LiveScanFeed.tsx:47-52; settings no account UI; signOut exists but unwired.
- p5-test-integrity-presweep.md: 3 e2e ORPHANS assert pre-demotion (goupc-ladder.spec.ts verified-badge, gpt-ladder-burst.spec.ts, polish-filter.spec.ts) - NOT in any task file list -> added MANDATORY e2e-demotion-sync gate step to P5 plan (orchestrator fixes after T1+T2 commit, before full e2e). Most other pre-demotion tests covered by task lists. False-positives excluded (two-provider-agreement e2e are AC5-protected).
CONCURRENCY: peaked at 7 agents (3 writers + 4 read-only scouts). Now 4 running (T2,T3,T4,P5b). All disjoint; commits serialized by orchestrator.

--- P5 WAVE COMPLETE: T1-T4 COMMITTED (2026-07-20) ---
T1 GPT demotion: 82ab775. T2 provider honesty+ripple audit: 0cf7f11. T3 cross-tier conflict (law-critical): fea577d.
T4 golden gates (real canAutoCount, no drift): 4e06fe7. All orchestrator diff-verified + gated (test:ledger 44/44, tsc clean).
T3 SAGA: scanCleaner.ts showed git-binary "Binary files differ" - INVESTIGATED: NOT corruption, it has an INTENTIONAL
 control-char INVISIBLE regex (strips zero-width/BOM from scans) that's always made it git-binary. My own tr-strip broke the
 NUL anchoring the \x00-\x08 range; recovered by restoring committed + byte-preserving Python re-apply (assert NUL count
 unchanged). Lesson: a git-binary source file isn't necessarily corrupt; never tr/sed a file with intentional control chars;
 verify via TESTS not textual diff. aliasMatcher 18/18 is the canary for INVISIBLE integrity.
T2 CRITICAL fix confirmed real: escalation win-selection (pipeline.ts:1259) gated on status==="verified" would have discarded
 every paid Go-UPC answer post-demotion -> repointed to goRun.outcome (settled). :1327/:1572/:1612 audited+documented (no pay-twice).
T5 badges (DISPLAY-only provenance, monolith site): DISPATCHED (base 4e06fe7, dedicated review on return).
REMAINING P5: (1) review+commit T5; (2) e2e-demotion-sync (goupc-ladder/gpt-ladder-burst/polish-filter specs - both demotion
 contracts now committed); (3) full phase gate (npm test/tsc/build/golden/ledger/firebase/e2e/Argus); (4) slim ultra review
 (2 finders trust-law + regression/test-integrity) over P5 diff; (5) close. Then P5b + P6 author (scouts all on disk).
P5b scout done (p5b-scout.md): catalogEntries write path net-new (getAdminDb in ai-lookup route = hook; catalogRepository read-only;
 rules deny client write). BIGGEST TRAP: MasterCandidate.productId is a tenant Product.id but CatalogEntry.id is master-namespace ->
 feeding raw = false conflict on every scan. Also resolver is sync/pure - live master read forces async or prefetch. CatalogEntry
 naming collision (db/types vs scanStore). ProvenanceTier 5-val vs CatalogEntry.verificationStatus 3-val = needs fresh mapping.

=== P5 IMPLEMENTATION COMPLETE + D8 FOLLOW-UP (2026-07-20, STOP POINT) ===
P5 Tasks 1-5 ALL COMMITTED: T1 82ab775, T2 0cf7f11, T3 fea577d, T4 4e06fe7, T5 5507d51.
D8 REGRESSION FIX: 2602e68 (full unit gate caught it - the focused tests missed it). T2's UPCitemdb-reuse
 dropped Plan D's barcodeDb leg when rung-0 rejects a code (stricter check-digit gate + no zero-pad retry
 vs barcodeDbProvider) -> 2-DB verified consensus collapsed to suggested. Fixed: reuse rung-0 when present,
 else fall back to barcodeDbProvider (upcItemDbResult ?? await lookupBarcodeDb(code)). route+pipeline 120/120, tsc clean.
UNIT GATE (pre-D8-fix) was 2763 pass / 2 fail (both = the D8 orphans in route.test.ts, now FIXED by 2602e68). tsc clean.
REMAINING P5 (see HANDOFF-2026-07-20.md for exact steps): (1) e2e-demotion-sync = 3 specs (goupc-ladder Test 1,
 gpt-ladder-burst, polish-filter) - exact goupc-ladder edits are in the handoff; (2) full phase gate (npm test now
 all-green post-fix, + build + test:golden + test:ledger + test:firebase + full e2e + Argus review-build); (3) slim
 ultra review 2 finders (trust-law + regression/test-integrity) over P5 diff 82ab775^..2602e68; (4) close + checkpoint.
THEN: P5b (docs/superpowers/plans TBD - author from p5b-scout.md) + P6 (author from p6-scout.md + p6-corpus-scout.md +
 p6-ops-scout.md). All scouts on disk. STOP requested by owner for a 1-hour handoff window.

## P5 CLOSE (2026-07-20, orchestrator session 2 - Fable 5)
- e2e-demotion-sync: 4 specs (goupc-ladder T1, gpt-ladder-burst, polish-filter, auto-decode) -> demoted contract; full e2e 55/55.
- Slim ultra review: Finder1 (trust/law) + Finder2 (regression/tests) + agy Flash/medium light pass + Argus review-build (PASS, 0 blockers).
  - Fixed: eval fixture honesty (tire-corpus rebase) + labeledClassCount denominator guard (d0dc416); D8b miss-path duplicate exact fetch (8ba2864, failing-first).
  - Adjudicated: agy "dead code" claim REFUTED vs source (settled rung-0 ends the free ladder, not the pipeline; Plan D reuse is live). D8 miss-path pad-variant refetch = intended design, now optimized.
- Gates at close: unit 2768/0, tsc 0, ledger 44/44, golden 2/2, firebase green (isolated; first run = worker-crash flake under parallel load), build clean, e2e 55/55, Argus PASS.
- Commits: 6225b88, d0dc416, 8ba2864, a4615be (P5+P5b plans), 47e34d4 (share tests + tools marker).
- P5b plan authored + adversarially reviewed (2 Blockers F2/F3 + 4 Importants patched into the plan) + Argus review-plan structural pass. NEXT: P5b T1+T2 (server write path) and T3 (transform) dispatched in parallel; T4 (scanStore, law-critical) after T3; then phase gate + ultra + qa:bots:tire.
