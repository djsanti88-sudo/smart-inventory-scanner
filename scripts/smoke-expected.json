{
  "_comment": "Expected capability contract after the 2026-08-21 decoder simplification. Update deliberately when the deployed contract changes; never silently to make smoke pass.",
  "aiLookup": {
    "openaiConfigured": true,
    "dailyLimit": 5000,
    "decodePath": ["tire_corpus", "retail_corpus", "learned_products", "master_catalog", "persisted_cache", "memory_cache", "gpt_5_4_mini"]
  }
}
