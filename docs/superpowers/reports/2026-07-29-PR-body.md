# PR: chore/docs-consolidation -> master

**Title:** `Product-readiness M0/M1/M3: docs consolidation, security fixes, activation, reconciliation value lever`

## Summary

This branch sits **on top of `audit-fixes`** (base commit `9a2a9030`), so merging it into `master` carries **both** bodies of work: the 7-lane audit-remediation series (`af/01`-`af/07`, already folded into `audit-fixes`) **and** the 25 commits of the `$150/mo` product-readiness push (docs grounding, M1 security/activation, the M3 reconciliation value lever, and a Next.js security upgrade). Together they turn the audit findings into fixes and the engineering-strong core into something closer to a sellable product, per `docs/superpowers/plans/2026-07-29-product-readiness-master-plan.md`.

## What's included

**Grounding & docs (M0)** - new `GUARDRAILS.md` (auto-loaded invariants) + `REPO_HEALTH.md` (branch/worktree sync-truth) + `docs/README.md` index; 60 historical plans/specs/reports archived with citation updates; overlapping living docs merged and contradictions resolved; `CLAUDE.md` slimmed 265 -> 232 lines (attack-panel approved); superseded tracked docs deleted.

**M1 security** - `catalogEntries` split off dispute/attribution data that was publicly readable unauthenticated (moved off the public doc), plus a backfill script with emulator integration tests; two rounds of deep-panel review (Codex sol-xhigh + Gemini) closed a re-leak on remerge and a legacy-backfill gap, plus 3 silent-failure findings from adversarial review.

**M1 activation/ops** - counted-quantity feedback panel now fires for every scan outcome (not just `known`); kill-switch state surfaced in Settings; "Clear local cache" now guards pending un-synced scans; new `/api/health` endpoint; F-01/F-07 prod Firestore rules/indexes redeployed and verified.

**M3 value** - reconciliation accepts any spreadsheet (not just Shop-Ware CSV) plus opt-in dollar-variance ("we found you $X") - the sales-demo asset, pulled forward per owner priority.

**Security** - Next.js bumped to 16.2.12 (7 CVEs closed) + npm audit bulk fix.

**Repo health** - 13 stale branches deleted, 11 clean worktrees reclaimed (all local, no pushes).

## Proof

- `tsc`: 0 errors
- Full Vitest suite: 3724 passing
- `test:ledger`: 45/45
- `test:firebase`: 113 passing
- `build`: 27/27 routes
- `test:e2e`: 58 passing
- 4 virtual shops (Lane 3 harness) proven live end-to-end

Review provenance: Fable 5 / Argus deterministic gates, a 3-Opus fix fleet, and a Codex (sol-xhigh) + Gemini deep-panel review of `firestore.rules` that **REJECTED** the first pass and was re-verified **CONFIRMED-FIXED** on the second; a holistic Opus review then certified the branch merge-ready.

## Owner action after merge

- [ ] Run the moderation backfill script with `--execute` against prod (owner-gated, real-data write)
- [ ] Create the uptime-monitor account (UptimeRobot/BetterStack) and point it at `/api/health`
- [ ] Provision a PostHog project key for the activation/funnel instrumentation
- [ ] Get lawyer review on the AI-drafted `docs/legal` drafts (ToS/Privacy/DPA) before charging real companies
- [ ] Triage remaining `npm audit` majors not covered by this Next.js bump

🤖 Generated with [Claude Code](https://claude.com/claude-code)
