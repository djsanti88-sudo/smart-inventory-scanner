{
  "done": true,
  "branch": "decoder-hardening-v1-local",
  "task": "Reverse-engineer + isolate + measure + plan the decode pipeline (no logic change)",
  "phases": {
    "phase_1_architecture": {
      "commit": "ab6a5db",
      "artifact": "docs/decode/ARCHITECTURE.md",
      "summary": "Mermaid of the REAL flow + cited file:line at every hop; the exact shared prompt (prompt.ts); per-model-call table (send -> get -> verify); EvidenceVerifier ladder; CrossCheck; decideDecode gates; store auto-count gate (scanStore.ts:1600 evidenceGatePassed + >=0.9 + firewall); cost/latency/cap/breaker; poison failure modes. Reconciles single_provider@~0.552 = maxConfidence(0.92)*0.6 from decode.ts:158 because only page-fetch returns within the 13s budget. States we do NOT train the models."
    },
    "phase_2_isolation": {
      "commit": "8eec438",
      "artifact": "src/services/decode/ (contract.ts, index.ts, README.md)",
      "summary": "Behavior-preserving: a documented front door that RE-EXPORTS the existing src/services/ai/* pipeline (no moves, no import churn) + the DecodeRequest->DecodeResult contract + DecodeProviderPort (Gemini/OpenAI/mock + future RAG all implement AiProvider) + DecodeEnrichPort. All tests stayed green."
    },
    "phase_3_eval_harness": {
      "commit": "f872610",
      "artifact": "src/eval/ + scripts/eval-decode.ts + docs/decode/eval-baseline.md",
      "summary": "Runnable eval over 9 real tires + poison. DEFAULTS to mock/offline fixtures (zero AI spend); --live is explicit + bounded to the dataset. Feeds fixtures through the REAL verifyEvidence -> decideDecode -> firewall -> store gate. Run: npx vitest run src/eval/eval.test.ts (also: npm run eval-decode).",
      "baseline": {
        "identity_accuracy_pct": 100,
        "auto_count_rate_pct": 33,
        "false_auto_count_rate_pct": 0,
        "specs_extracted_pct": 89,
        "note": "auto-count limited to STRONG-prefix-family tires because every decode is single-provider; poison never auto-counts (FALSE=0)."
      }
    },
    "phase_4_improvement_plan": {
      "commit": "7d2c5d7",
      "artifact": "docs/decode/IMPROVEMENT_PLAN.md",
      "summary": "Analysis only. Prioritized by impact/effort: P0 catalog/flywheel short-circuit (skip AI); P1 genuine two-provider cross-check (count page-fetch + one model as agree) to break the 0.552 ceiling; P2 RAG provider on the Provider port (prefix table + catalog flywheel corpus); P3 prompt upgrades (JSON-retry, speed-rating, anti-poison/exact-code); P4 evidence strictness to keep FALSE auto-count at 0. Nothing implemented."
    }
  },
  "verification": {
    "tsc": "clean (0 errors)",
    "eslint": "clean (0 errors, 4 pre-existing warnings)",
    "vitest": "609 passed / 30 skipped / 0 failed (incl. 3 eval tests)",
    "qa_bots": "8 passed (role-security-leak + export-leak findings [])",
    "playwright": "19 passed",
    "eval_mock": "FALSE auto-count 0% asserted; baseline written to docs/decode/eval-baseline.md"
  },
  "invariants_preserved": {
    "no_decode_logic_changed": true,
    "isolation_behavior_preserving": true,
    "eval_defaults_to_no_live_ai": true,
    "poison_745125495781_blocks": true,
    "firewall_corroboration_gates_intact": true,
    "customer_privacy_gate_intact": true
  },
  "notes": "Local only. Nothing pushed/merged/deployed. Phase 2 added a facade only (zero behavior change). Eval fixtures are REPRESENTATIVE (real brands/codes + plausible specs), not captured live transcripts, to respect money-safety; --live measures real extraction within the cap. vitest.config gained a test-only include for src/eval."
}
